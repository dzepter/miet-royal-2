/**
 * Gemeinsame UI-Bausteine für apps/web und apps/staff.
 *
 * Phase 0 legt nur den Platz fest: Gemeinsame Komponenten entstehen,
 * sobald echte Oberflächen gebaut werden (UX_RULES.md gilt dann als
 * verbindliche Grundlage). Kein visuelles Branding in Phase 0.
 */
export const UI_PACKAGE_PLACEHOLDER = true;
